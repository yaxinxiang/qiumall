spring:
  datasource:
    url: jdbc:mysql://101.34.5.204:3306/qiumall?useUnicode=true&characterEncoding=UTF-8&useSSL=false
    username: qiumall
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true

server:
  port: 8888

# 定义部署的服务器静态资源位置
# 部署的时候需要修改这个自定义的静态文件夹或者加载另外的配置文件
static :
  profile: E:/IDEA_project/teststatic